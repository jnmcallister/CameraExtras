# CameraExtras

Allows the user to freeze the camera and to hide UI elements

Default keybinds:
- B: Freeze the camera
- L: Turn off UI elements

Notes:
- When going into a level transition, the camera will unfreeze
- When the UI elements are turned off, the fade effect during level transitions is skipped